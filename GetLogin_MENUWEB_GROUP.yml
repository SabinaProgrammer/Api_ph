info:
  name: GetLogin_MENUWEB_GROUP
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/inter/GetLogin_MENUWEB_GROUP"
  body:
    type: json
    data: |-
      {
        "id": "chat",
        "password": "is007"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
