info:
  name: GetMENUWEB
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/Inter/GetMENUWEB"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
