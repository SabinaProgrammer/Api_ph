info:
  name: GetToken
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/Authen/GetToken"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
