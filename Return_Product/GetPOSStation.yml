info:
  name: GetPOSStation
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/Inter/GetPOSStationID?pcid=chat"
  params:
    - name: pcid
      value: chat
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
