info:
  name: GetSearchReturnByID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/Inter/GetSearchReturnByID?docno=RA26001168"
  params:
    - name: docno
      value: RA26001168
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
