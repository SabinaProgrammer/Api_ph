info:
  name: DeleteReturn
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/api/Inter/DeleteReturn"
  body:
    type: json
    data: |-
      {
        "DOCNO": "RT26000001",
        "UserID": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
