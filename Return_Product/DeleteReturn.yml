info:
  name: DeleteReturn
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/api/Inter/DeleteReturn"
  body:
    type: json
    data: |-
      {
        "DOCNO": "RA26001168",
        "UserID": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
