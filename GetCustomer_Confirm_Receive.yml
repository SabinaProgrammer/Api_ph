info:
  name: GetCustomer_Confirm_Receive
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/Inter/GetCustomer?pcid=000001"
  params:
    - name: pcid
      value: "000001"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
