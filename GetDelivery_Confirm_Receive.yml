info:
  name: GetDelivery_Confirm_Receive
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/Inter/GetDelivery?id=F001"
  params:
    - name: id
      value: F001
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
