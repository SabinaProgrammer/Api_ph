info:
  name: SaveConfirm_Receive
  type: http
  seq: 5

http:
  method: POST
  url: http://192.168.5.30:8071/api/PH/Save_Confirm_Receive
  body:
    type: json
    data: |-
      {
        "userID": "F001",
        "dvids": [
          "DS24000001"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
