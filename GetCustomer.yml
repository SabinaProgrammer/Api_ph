info:
  name: GetCustomer
  type: http
  seq: 6

http:
  method: GET
  url: http://192.168.5.30:8071/api/PH/GetCustomer?pcid=000005
  params:
    - name: pcid
      value: "000005"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
